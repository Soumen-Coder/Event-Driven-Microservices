spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:D:/Event-Driven-Microservices/microservices-base/microservices-base/config-server-repository
          clone-on-start: true
    fail-fast: true